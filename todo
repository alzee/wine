trix editor upload img

vichuploader attribute cause apiplatform error

collectionField default have one, least one

default (choice) value for default roles?
validator in orderItems not work in orders form?

err:
  - out of range
  - permission

chart

have to use setTimeout() in js?
why repo->find(1) give uninitialized? $this->getUser()->getOrg() too. Thought ...getOrg()->getType() works.

# vim:ft=yaml
